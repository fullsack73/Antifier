# Dependencies needed by the Python test suite.
# Keep this smaller than requirements-pypi.txt so CI does not install
# heavyweight runtime-only ML packages on every push.
flask
flask-cors
numpy==2.3.5
pandas<3
scipy
scikit-learn
tensorflow==2.20.0
yfinance==0.2.66
pyportfolioopt==1.5.6
psutil==7.0.0
pytest
